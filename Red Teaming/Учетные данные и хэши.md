## Брутфорс учетных данных в веб-приложениях
- Перечисление пользователей часто возможно на странице авторизации/регистрации: необходимо проверять ответы HTTP-сервера (HTTP-коды, сообщения об ошибках, время ответа) при вводе существующих и несуществующих логинов. Если они отличаются, таким образом можно получить существующие логины
- Для обхода HTTP-ответа "429 Too many requests" можно использовать расширение Burp Suite "429 Bypasser" ([частые пользовательские заголовки](https://gist.github.com/kaimi-/6b3c99538dce9e3d29ad647b325007c1))
- Если возможно, желательно сначала сбрутить логины, потом сбрутить пароли
- Для брутфорса по времени ответа можно использовать Burp Suite Intruder и колонки "Response recieved", "Response completed"
- Обход ограничения по IP иногда может быть снят входом в аккаунт, атакующий может использовать свой аккаунт для сброса ограничения
- Атакующий может использовать подстановку учетных данных (credential stuffing). Атакующий использует утекшие учетные данные на других сайтах, так как пользователь может переиспользовать их
- Если аккаунты блокируются после нескольких неудачных попыток входа, атакующий может использовать это для перечисления существующих пользователей
### Инструменты
- Burp Suite Intruder Cluster Bomb (несколько полезных нагрузок)	
- Burp Suite Intruder Sniper (одна полезная нагрузка)
- Hydra 
	- POST-форма:`hydra -L <login wordlist: /usr/share/wordlists/metasploit/unix_users.txt> -P <password wordlist: /usr/share/wordlists/metasploit/unix_users.txt> -s <port> <target> http(s)-post-form -m "/admin/index.php:username=^USER^&password=^PASS^:H=<cookie/specific header>:F/S=<fail/success message>"`
- OWASP ZAP
- nmap: `nmap --script ssh-brute --script-args userdb=<login wordlist>,passdb=<password wordlist> <target>`
- [patator](https://github.com/lanjelot/patator): `patator ssh_login host=<target> user=admin password=FILE0 0=<password wordlist> -x ignore:fgrep='Permission denied'`
- [medusa](https://github.com/jmk-foofus/medusa)
- metasploit (MSSQL, MySQL, VNC)
## Извлечение учетных данных из ОС
-  LinPeas
- WinPeas
- Mimikatz
- [KeePass](https://github.com/JorianWoltjer/keepass-dump-extractor)
## Восстановление паролей из хэшей
- John the Ripper
- Hashcat и Hashid: `hashid <hash>` (детект типа хэша); `hashcat -m <hash type> -a <attack mode> <hash file> <password wordlist>`
- Онлайн сервисы: https://spy-soft.net/kak-rasshifrovat-xesh-bez-dolgix-vychislenij/
## Восстановление паролей и ключей из памяти процессов
- Mimipenguin
- truffleproc
- Вручную:
	1. `ps -ef | grep "authenticator"` (ищем в процессах строку "authenticator")
	2. `./dump-memory.sh 2027` (делаем дамп найденного процесса)
	3. `strings *.dump | grep -i "pass"` (в дампе ищем учетные данные)
## Сброс пароля
### Windows
- ntpasswd: https://pogostick.net/~pnh/ntpasswd/, https://adamtheautomator.com/ntpasswd/
- Kon-Boot: https://www.piotrbania.com/all/kon-boot/
## Учетные данные по умолчанию
- [Default Credentials Cheat Sheet](https://github.com/ihebski/DefaultCreds-cheat-sheet/tree/main#default-credentials-cheat-sheet) - ПО на Python для поиска дефолтных кредов в CSV-файле, CSV-файл лежит в проект